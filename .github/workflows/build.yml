name: Build SANA-WM Demo Image

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: sana-wm-demo-build
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Get files
        uses: actions/checkout@v4

      - name: Set image name
        id: image
        run: |
          BASE_IMAGE="ghcr.io/${GITHUB_REPOSITORY,,}"
          echo "demo=$BASE_IMAGE:demo1" >> "$GITHUB_OUTPUT"
          echo "Using image: $BASE_IMAGE:demo1"

      - name: Set up Docker builder
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub container storage
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and publish demo image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.image.outputs.demo }}
          provenance: false
