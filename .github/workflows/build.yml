name: Build SANA-WM Full Loop R2 Image

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: sana-wm-full-loop-r2-build
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Get files
        uses: actions/checkout@v4

      - name: Set image name
        id: image
        run: |
          BASE_IMAGE="ghcr.io/${GITHUB_REPOSITORY,,}"
          echo "e2e=$BASE_IMAGE:demo-r2-1" >> "$GITHUB_OUTPUT"
          echo "Using image: $BASE_IMAGE:demo-r2-1"

      - name: Set up Docker builder
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub container storage
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and publish full loop image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.image.outputs.e2e }}
          provenance: false
